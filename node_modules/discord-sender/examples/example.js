<!DOCTYPE html>
<html>
<body>


<p id="demo"></p>

<script>
function embed(channel, embed) {

const obj = JSON.parse(embed);
if (channel == "" || channel == undefined) throw new TypeError("Please Provide A Channel ID");
if (embed == "" || embed == undefined) throw new TypeError("Embed Is Undefined");
if (obj == "" || obj == undefined) throw new TypeError("JSON Format Error");

var color = obj.color;
var title = obj.title;
var url = obj.url;
var thumbnail = obj.thumbnail;
var author = obj.author;
var authorIcon = obj.authorIcon;
var authorUrl = obj.authorUrl;
var image = obj.image;
var description = obj.description;
/*color*/
if(color == "" || color == undefined){
  color = "";
}else {
  color = `&color=${color}`
}
/*title*/
if(title == "" || title == undefined){
  title = "";
}else {
  title = `&title=${title}`
}
/*url*/
if(url == "" || url == undefined){
  url = "";
}else {
  url = `&url=${url}`
}
/*thumbnail*/
if(thumbnail == "" || thumbnail == undefined){
  thumbnail = "";
}else {
  thumbnail = `&thumbnail=${thumbnail}`
}
/*author*/
if(author == "" || author == undefined){
  author = "";
}else {
  author = `&author=${author}`
}
/*authorIcon*/
if(authorIcon == "" || authorIcon == undefined){
  authorIcon = "";
}else {
  authorIcon = `&authorIcon=${authorIcon}`
}
/*authorUrl*/
if(authorUrl == "" || authorUrl == undefined){
  authorUrl = "";
}else {
  authorUrl = `&authorUrl=${authorUrl}`
}
/*image*/
if(image == "" || image == undefined){
  image = "";
}else {
  image = `&image=${image}`
}
/*description*/
if(description == "" || description == undefined){
  description = "";
}else {
  description = `&description=${description}`
}

/*fetch*/
fetch(`https://shell.joelee.ga/embed?channel=${channel}${title}${color}${url}${thumbnail}${author}${authorIcon}${authorUrl}${image}${description}`)


}


var value = '{"title":"joelee", "color":"RANDOM", "description":"okok", "url":"https://joelee.ga", "image": "https://cdn.discordapp.com/emojis/792605503477121025.gif?v=1", "thumbnail":"https://cdn.discordapp.com/emojis/792605503477121025.gif?v=1", "author": "joelee", "authorIcon":"https://cdn.discordapp.com/emojis/792605503477121025.gif?v=1", "authorUrl": "https://joelee.ga"}'
embed('705036476584296500', value)
</script>

</body>
</html>
